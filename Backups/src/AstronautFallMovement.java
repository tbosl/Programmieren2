public class AstronautFallMovement {
    // In astronaut before moveToPostion()
    // speedInPixel = position.getY() != targetPosition.getY() ? 2 : 0.25;

    // var in AstronautMovementPatterns
    //    static final double GROUND_LEVEL = GameView.HEIGHT - 50;
    //
    //    First Line of nextTargetPosition
    //    if (astronaut.getPosition().getY() < GROUND_LEVEL) {
    //       return new Position(astronaut.getPosition().getX(), GROUND_LEVEL);
    //    }
}
